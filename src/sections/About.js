import React from 'react';
import { Title } from '../components';

const About = () => {
    return (
        <section id='about'>
            <Title>About</Title>
        </section>
    );
};

export default About;
