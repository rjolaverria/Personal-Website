import React from 'react';
import { Container, Title } from '../components';

const About = () => {
    return (
        <Container>
            <Title>About</Title>
        </Container>
    );
};

export default About;
