import React from 'react';
import { Container, Title } from '../components';

const Home = () => {
    return (
        <Container>
            <Title linePosition='right'>Home</Title>
        </Container>
    );
};

export default Home;
